---
import { resolveAsset } from '@utils/resolveAssets';

interface Props {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props;
---

<img src={resolveAsset(src)} alt={alt} class="article-thumbnail" />

<style>
  .article-thumbnail {
    display: block;
    width: 60%;
    height: auto;
    max-height: 160px;
    object-fit: contain;
  }
</style>
