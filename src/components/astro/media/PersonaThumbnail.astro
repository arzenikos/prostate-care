---
import { resolveAsset } from '@utils/resolveAssets';

interface Props {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props;
---

<img src={resolveAsset(src)} alt={alt} class="persona-thumbnail" />

<style>
  .persona-thumbnail {
    display: block;
    width: 100%;
    height: auto;
    max-height: 160px;
    object-fit: contain;
  }
</style>
